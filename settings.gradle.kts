plugins {
    // Auto-provisions a JDK 21 toolchain if one is not already installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "CivitasCraft"
